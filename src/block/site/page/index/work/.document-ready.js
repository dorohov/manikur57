/*$(document.body).on('click', '._iwb__tabs-nav a', {}, function(event){
	event.preventDefault();

	var btn = $(this);
	var id = btn.attr('href');

	$(id).find('._iwb__tab-preview .tab-pane').removeClass("active in").eq(0).addClass('active in');
	//$(id).find('.tabs-price__nav li.active').removeClass("active");
})*/